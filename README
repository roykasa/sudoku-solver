solving sudoku using python. open the file using any editor and replace the string of numbers with the same pattern of numbers as that in the sudoku puzzle you are trying to solve. Save and run the file.